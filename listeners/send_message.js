'use strict'

const salonService = require("../services/salonService");
const user_data_service = require("../services/user_data_service");

module.exports = async (props, event, api) => {
    console.log(event)
    // return salonService.new()
}